import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.allinonetools.app',
  appName: 'All In One Tools',
  webDir: 'build'
};

export default config;
